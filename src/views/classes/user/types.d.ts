export interface UserParams {
  q: string,
  role: string,
  plan: string,
  status: string,
  options: object,
}


export interface StudentParams {
  q: string,
  classCode: string,
  plan: string,
  status: string,
  options: object,
}
